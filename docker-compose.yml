services:
  web:
    container_name: stock-info-app-prod
    build:
      context: .
      dockerfile: Dockerfile       # 기본 Dockerfile (멀티 스테이지) 사용
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always                # 장애 시 자동 재시작
